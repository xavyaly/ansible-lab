Home Work:

Create a playbooks:

To add the authorizedkeys "ssh-keygen"	if not present

To add multiple user and add into docker group

To install python-minimal

To install docker, jenkins, mysql, nodejs etc

To install filebeat through ansible

To restart the services, stop the services, start the services, ping the servers


